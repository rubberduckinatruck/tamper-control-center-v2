# Draft userscripts

Incomplete experiments belong in `drafts/<category>/` and must use `@cc-status draft`. Drafts are syntax- and metadata-checked but excluded from `scripts.json` and every published catalog view.

Move a finished draft to `scripts/<category>/`, change its status to `live` or `beta`, add canonical update/download URLs, and run `npm run check`.
